{
  "name": "scentwise-ai",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "test": "npx playwright test",
    "test:mobile": "npx playwright test --project=mobile"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
