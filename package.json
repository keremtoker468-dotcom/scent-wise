{
  "name": "scentwise-ai",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "dev:local": "node scripts/dev-server.js",
    "seo:build": "cd seo && npx @11ty/eleventy",
    "seo:serve": "cd seo && npx @11ty/eleventy --serve",
    "seo:deploy": "cd seo && npx @11ty/eleventy && cp -r _site/perfumes _site/brands _site/notes ../public/",
    "buy-links": "node scripts/add-buy-links.js",
    "test": "jest"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "jest": "^29.7.0"
  }
}
